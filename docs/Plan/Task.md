# セキュリティ強化・RBAC実装 タスクリスト

- [x] 1. 準備とUIの更新
    - [x] `index.html` に CryptoJS のCDNを追加
    - [x] `index.html` のナビゲーションヘッダーにユーザー権限を切り替えるセレクタ（院長・歯科医師・受付）を追加
- [x] 2. データベース層（暗号化と監査ログベース）の実装
    - [x] `data.js` 内に環境変数・シークレットキーの定義とAES暗号化/復号化関数を追加
    - [x] 既存の患者データを暗号化フォーマットにマイグレーション（移行）するスクリプトを追加
    - [x] `AuditLog` 配列と、ログを書き込む関数を追加
- [x] 3. RBACとミドルウェア・ガードの適用
    - [x] `app.js` 内にRBACミドルウェアフックを追加し、受付(`staff`)権限のユーザーが「カルテ管理」にアクセスした際に警告を表示してブロックする
- [x] 4. セキュリティ脆弱性の修正とオーディットログの挿入
    - [x] `app.js` の `innerHTML` を使ったXSS脆弱性箇所をサニタイズ（エスケープ）関数を使うように修正
    - [x] 「新規患者登録」「カルテ登録」のアクション時に監査ログを出力する処理を追加
- [x] 5. 検証とドキュメント化
    - [x] `walkthrough.md` で修正内容と適用前後のコードの違いを解説したドキュメントを作成

# 認証機能・権限管理・タグ改修 タスクリスト

- [x] 6. ユーザー情報とログインの準備
    - [x] `test-userpass.md` ファイルを作成し、3段階の権限のアカウント情報を記載する
    - [x] `data.js` の mock data に `users` 配列を追加し、上記3つのアカウントを設定する
- [x] 7. ログイン画面・ログアウト機能の実装
    - [x] `index.html` にログイン画面のフルスクリーンUIを追加し、初期表示に設定する
    - [x] `app.js` にログイン検証ロジックを実装し、成功時にメイン画面へ遷移させる
    - [x] ログアウトボタンを追加し、クリックでログイン画面へ戻す
- [x] 8. 管理画面（アクセス者管理）の実装
    - [x] `index.html`にシステム管理画面を追加し、現在のユーザー一覧と追加/編集フォームを構築する
    - [x] 既存のIDを入力した際はエラーにせず情報を上書きできるよう `app.js` にロジックを実装する
- [x] 9. 患者タグ・ステータス編集機能の追加
    - [x] 新規患者登録時に「タグ入力（カンマ区切り）」ができるようフィールドを追加
    - [x] カルテ管理（SOAP）作成フォーム内に「ステータス」および「タグ」を上書き保存する機能を実装
    - [x] ステータスが変更された場合、カルテ履歴にはバッジとして記録を残し、CRM上は最新ステータスとして反映させる
- [x] 10. ドキュメントや小修正の対応
    - [x] `README.md` を技術仕様からエンドユーザー向けカタログ仕様へ書き換え
    - [x] ダッシュボードの機能説明を記述
    - [x] プロジェクト内の `Plan/` ディレクトリにタスク進行とウォークスルーの履歴を格納する

- [x] 2026-04-15: カルテ機能と紹介ネットワークの連携強化 (commit: 66c8190)
- [x] 2026-04-15: ログインできないバグの修正 (commit: 0388bc7)
- [x] 2026-04-15: カルテ追加項目の欠落再実装 (commit: b77503e)
